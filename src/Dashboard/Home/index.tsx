//import { getAuth } from "firebase/auth";
//import { app } from "@/firebase";

const Home = () => {
  //const auth = getAuth(app);

  return (
    <div className="">
      <h1>Home</h1>
    </div>
  );
};

export default Home;
